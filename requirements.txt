# requirements.txt - Dependências do Network Scanner

# Utilitários de rede
ipaddress>=1.0.23

# Para requisições HTTP (consulta API de MACs)
requests>=2.31.0

# Cores no terminal (opcional, mas bonito)
colorama>=0.4.6

# Para validação e parsing
netaddr>=0.8.0

# Para interface gráfica opcional (comentado, instale se quiser)
# tkinter  # Já vem com Python

# Para testes (opcional)
pytest>=7.4.0

# Para melhor formatação de tabelas
tabulate>=0.9.0

# Para progress bars em scans longos
tqdm>=4.66.0colorama>=0.4.6
